import React from 'react';
import Login from '../Presentational/Login';

const LoginContainer = () => {
  const tmp = 0; // 임시 변수. 아래에 api 콜들 추가 예정.

  return <Login />;
};

export default LoginContainer;
